package test;

public class WW
{
	private int x;  // with in the class
	// default scope with in the package
	int y;  // default scope or friend scope
	protected int z=999;
	// can access with in the package and also 
	// outside the package through inheritance
	
	public int s = 33; //access in any package
}
